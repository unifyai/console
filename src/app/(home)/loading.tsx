import LoadingScreen from '@/components/Layout/LoadingScreen';

/** Full-screen brand loader while (home) route segments resolve. */
export default function HomeLoading() {
  return <LoadingScreen />;
}
