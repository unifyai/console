import LoadingScreen from '@/components/Layout/LoadingScreen';

/** Brand loader while the assistants page bootstraps coordinator data. */
export default function AssistantsLoading() {
  return <LoadingScreen />;
}
